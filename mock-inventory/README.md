# Mock gRPC Inventory Server for the API Gateway

After cloning this repo, change directory into the 'grpc-mock' directory and run the command:
### `npm install`

Then to start the server run the following command:
### `node server.js`